{
  "name": "__PACKAGE_NAME__",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint \"src/**/*.ts\"",
    "check-types": "tsc --noEmit",
    "typecheck": "npm run check-types"
  },
  "devDependencies": {
    "__PACKAGE_NAMESPACE__/eslint-config": "*",
    "@types/jest": "latest",
    "jest": "latest",
    "ts-jest": "latest"
  }
}
