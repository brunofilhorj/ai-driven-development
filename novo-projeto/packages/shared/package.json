{
  "name": "@poupig/shared",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@poupig/eslint-config": "*",
    "@types/jest": "latest",
    "jest": "latest",
    "ts-jest": "latest"
  },
  "dependencies": {
    "uuid": "^11.1.1"
  }
}
